import { UserProfileDirective } from './user-profile.directive';

describe('UserProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new UserProfileDirective();
    expect(directive).toBeTruthy();
  });
});
